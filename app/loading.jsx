import styles from "@/app/styles/common.module.css";

const Loading = () => {
  return (
    <div className={styles.loading}>
      <h3 className={styles.text}>Loading,please wait...</h3>
    </div>
  );
};

export default Loading;
